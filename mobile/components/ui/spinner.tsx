import {
  ActivityIndicator,
  ActivityIndicatorProps,
  View,
  ViewProps,
} from "react-native";

// Spinner is an ActivityIndicator
export const Spinner = ActivityIndicator;

export const SpinnerGroup = View;

export type SpinnerProps = ActivityIndicatorProps;
export type SpinnerGroupProps = ViewProps;
